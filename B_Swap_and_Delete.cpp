#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main(){
    int t;
    cin >> t;
    while(t--){
        string s;
        cin>>s;

        int zeros=0;
        int ones=0;
        for(int i=0 ; i<s.size() ; i++){
            if(s[i]=='0') zeros++;
            else ones++;
        }

        
        
    }
    return 0;
}
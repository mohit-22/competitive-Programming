#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main(){
    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        string s;
        cin>>s;

        if(s.length()<=2){
            cout<<s<<endl;
            continue;
        }

        if(s[0]==0 || s[s.length()]==0){
            cout<<
        }
    }
    return 0;
}